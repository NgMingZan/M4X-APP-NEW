# M4X Theme v3.0.2 Fix

- Sửa số dư M4X COIN không cập nhật ngay sau Giftcode, Quest và Airdrop.
- Sửa upload theme gửi cột cũ `author`/`name` không tồn tại trong schema v3.
- Upload theme giờ chỉ dùng `owner_id` và `title`.
