M4X ARENA v3.3.0 — FIX UNRESOLVED REFERENCE consume

Lỗi GitHub Actions:
ArenaGame.kt:98:42 Unresolved reference 'consume'

Nguyên nhân:
Compose hiện tại không còn cần import:
androidx.compose.ui.input.pointer.consume

Hàm change.consume() vẫn được giữ nguyên.

Cài patch trong Termux:

cd ~/storage/downloads
unzip -o M4X_ARENA_CONSUME_FIX.zip

cp -a M4X_ARENA_CONSUME_FIX/. \
~/storage/downloads/M4X-APP-NEW-RUST/

cd ~/storage/downloads/M4X-APP-NEW-RUST
git config --global --add safe.directory "$PWD"
git add app/src/main/java/com/m4xtheme/app/ArenaGame.kt
git commit -m "Fix Arena pointer consume import"
git push origin main

Không cần chạy lại SQL Supabase.
